<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/navigation_graph"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/homeFragment"
        android:name="com.example.cardapp.features.home.HomeFragment"
        android:label="Home">
        <action
            android:id="@+id/action_to_details"
            app:destination="@id/detailsComposable" />
    </fragment>

    <!--<composable
        android:id="@+id/detailsComposable"
        android:name="com.example.DetailsScreen"
        app:route="details/{itemId}">
        <argument
            android:name="itemId"
            app:argType="integer" />
    </composable>-->
    <fragment
        android:id="@+id/detailsComposable"
        android:name="com.example.cardapp.features.details.DetailsFragment"
        android:label="Details">
        <argument
            android:name="itemId"
            app:argType="integer" />
    </fragment>

</navigation>